# shot-review run — 2026-07-17

## Step 1 — 01-lightbox-component

```
node --check: lightbox OK / dashboard OK / chrome OK / server OK. curl http://127.0.0.1:4599/lightbox.js -> HTTP/1.1 200 OK, Content-Type: application/javascript, served==source (10524 bytes, 3x window.BrainLightbox). curl /watch/core-cli?brain=<abs> -> HTTP/1.1 200 OK with <script src="/lightbox.js"></script> ordered before <script src="/dashboard.js"></script>.
```

## Step 2 — 02-annotation

```
node --check: OK on lightbox.js, chrome.js, dashboard.js, server.js, brain-data.js. Temp-brain test: missing annotations.json -> []; addAnnotation returns {id,shot,x:42.4,y:71.9,note,at,shotMtimeMs,shotSize,superseded:false}; list after add -> 1 open; bad shot + traversal throw clean Error; shot rewrite -> superseded:[true]; shot delete -> superseded:[true]; corrupt JSON -> clean throw. Curl @4599: POST annotate valid shot HTTP 200 (returns annotation rec); invalid shot HTTP 400 {error:no screenshot...}; unknown feature HTTP 404; GET watch context includes annotations array with the pin. Fixture left clean (annotations.json removed).
```

## Step 3 — 03-shots-notes-cli

```
Added `brain shots notes <feature>` subcommand (cmdShotsNotes) and open-notes
counts on `brain shots [<feature>]` (openNotesByRel). Verified:

$ node bin/brain.js shots notes --help --brain .brain -> exit 0, help block
$ node bin/brain.js shots notes shot-review --brain .brain
notes: 0 annotations for shot-review
help[2]: Run `brain watch shot-review` and pin a note...; Run `brain shots shot-review`...
exit 0

$ node bin/brain.js shots notes nosuch --brain .brain
error: no feature "nosuch"
help[1]: known slugs: core-cli, brain-review, shot-review
exit 1

$ node bin/brain.js shots notes shot-review --extra-flag --brain .brain
error: unknown flag --extra-flag for `shots notes`
exit 2

Populated path (throwaway brain in scratchpad, testfeat + real png +
addAnnotation x2): shots notes showed both open with pin `42.4%,71.9%` and
`10%,20%`, long note truncated to 120 chars with a trailing ellipsis; `shots
testfeat` showed a `notes` column with count 2. After `shots add` re-captured
the same step (new png bytes/mtime), both flipped to superseded (0 open, 2
superseded) and the `notes` column disappeared from `shots testfeat` (0 open
== column omitted, matches the no-annotations quiet path). Corrupt
annotations.json in the same throwaway brain: both `shots notes testfeat` and
`shots testfeat` opError with "corrupt annotations JSON at <path>: ..." exit 1.

$ node bin/brain.js skill --check --brain .brain -> "skill: skills/brain/SKILL.md is up to date" exit 0
$ node bin/brain.js shots --brain .brain -> unchanged 3-row listing, still works.
stderr stayed empty on all runs; stdout TOON only.
```
