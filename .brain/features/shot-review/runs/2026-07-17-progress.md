# shot-review run — 2026-07-17

## Step 1 — 01-lightbox-component

```
node --check: lightbox OK / dashboard OK / chrome OK / server OK. curl http://127.0.0.1:4599/lightbox.js -> HTTP/1.1 200 OK, Content-Type: application/javascript, served==source (10524 bytes, 3x window.BrainLightbox). curl /watch/core-cli?brain=<abs> -> HTTP/1.1 200 OK with <script src="/lightbox.js"></script> ordered before <script src="/dashboard.js"></script>.
```

## Step 2 — 02-annotation

```
node --check: OK on lightbox.js, chrome.js, dashboard.js, server.js, brain-data.js. Temp-brain test: missing annotations.json -> []; addAnnotation returns {id,shot,x:42.4,y:71.9,note,at,shotMtimeMs,shotSize,superseded:false}; list after add -> 1 open; bad shot + traversal throw clean Error; shot rewrite -> superseded:[true]; shot delete -> superseded:[true]; corrupt JSON -> clean throw. Curl @4599: POST annotate valid shot HTTP 200 (returns annotation rec); invalid shot HTTP 400 {error:no screenshot...}; unknown feature HTTP 404; GET watch context includes annotations array with the pin. Fixture left clean (annotations.json removed).
```

## Step 3 — 03-shots-notes-cli

```
Added `brain shots notes <feature>` subcommand (cmdShotsNotes) and open-notes
counts on `brain shots [<feature>]` (openNotesByRel). Verified:

$ node bin/brain.js shots notes --help --brain .brain -> exit 0, help block
$ node bin/brain.js shots notes shot-review --brain .brain
notes: 0 annotations for shot-review
help[2]: Run `brain watch shot-review` and pin a note...; Run `brain shots shot-review`...
exit 0

$ node bin/brain.js shots notes nosuch --brain .brain
error: no feature "nosuch"
help[1]: known slugs: core-cli, brain-review, shot-review
exit 1

$ node bin/brain.js shots notes shot-review --extra-flag --brain .brain
error: unknown flag --extra-flag for `shots notes`
exit 2

Populated path (throwaway brain in scratchpad, testfeat + real png +
addAnnotation x2): shots notes showed both open with pin `42.4%,71.9%` and
`10%,20%`, long note truncated to 120 chars with a trailing ellipsis; `shots
testfeat` showed a `notes` column with count 2. After `shots add` re-captured
the same step (new png bytes/mtime), both flipped to superseded (0 open, 2
superseded) and the `notes` column disappeared from `shots testfeat` (0 open
== column omitted, matches the no-annotations quiet path). Corrupt
annotations.json in the same throwaway brain: both `shots notes testfeat` and
`shots testfeat` opError with "corrupt annotations JSON at <path>: ..." exit 1.

$ node bin/brain.js skill --check --brain .brain -> "skill: skills/brain/SKILL.md is up to date" exit 0
$ node bin/brain.js shots --brain .brain -> unchanged 3-row listing, still works.
stderr stayed empty on all runs; stdout TOON only.
```

## Step 4 — 04-verification-walk

```
Verdict: PASS. Browser-walked /watch/shot-review end-to-end with Playwright (fresh chromium, --disable-extensions) against a freshly-spawned review server (killed a stale one on 4517 whose /lightbox.js 404'd — pre-dated the route entirely; verified the fresh server's /lightbox.js byte-identical to lib/review/lightbox.js before driving anything).

Golden path (7/7 pass): 01 dashboard renders empty-shots state, bootstrapped as first content shot; 02 thumbnail click opens lightbox in-page (context.pages().length unchanged, asserted); 03 ArrowRight updates counter 1/2->2/2 and moves active filmstrip thumb; 04 Annotate toggle + click ~40%,~70% + note "button misaligned — test pin" -> POST /watch/shot-review/annotate returned 200, numbered pin rendered; 05 `brain shots notes shot-review --brain .brain` showed the annotation open with pin coords 39.9%,69.9%; 06 Esc closed the overlay cleanly; 07 re-adding (overwriting) the annotated shot flipped shots notes to superseded.

shots notes verbatim (after step 04): "notes: 1 annotations for shot-review (1 open, 0 superseded)" / features/shot-review/screenshots/02-lightbox-open.png,"39.9%,69.9%",button misaligned — test pin,...,open
shots notes verbatim (after step 07 supersede): "notes: 1 annotations for shot-review (0 open, 1 superseded)" / same annotation now status: superseded.

Error path (E1, primary Playwright variant): dropped a pending pin + typed a note, killed the actual review-server listener (verified via `lsof -ti:4517 -sTCP:LISTEN`), then clicked Save -> inline error ".bl-note-err" showed "Could not save: Failed to fetch", note text preserved verbatim, no annotation persisted (confirmed against annotations.json).

Console: two browser-native "Failed to load resource: net::ERR_INCOMPLETE_CHUNKED_ENCODING / ERR_CONNECTION_REFUSED" diagnostics fired only on the E1 step (SSE stream + POST hitting the killed server) — grepped lib/review/ and bin/ for both strings, neither appears in app source, so treated as network/environment artifacts of the intentional kill, not app jsErrors. Zero console errors on any golden-path step. No networkErrors bucket entries (the killed-server request failed at the connection level, never got an HTTP response).

Deviation/incident: first E1 attempt used `lsof -ti:4517` (all connections, not just LISTEN) and killed the wrong PID — the user's "Comet" browser process that had an established connection to port 4517, not the review server. That process was terminated as a side effect and could not be restarted by this agent. Caught immediately (server never actually went down, so the "error" path silently succeeded instead, creating one spurious real annotation) — deleted that stray annotation from annotations.json and reran E1 correctly with `-sTCP:LISTEN` targeting only the true listener PID. Flagging so the user is aware their browser process was closed.

Full doc: .brain/features/shot-review/verifications/2026-07-17.md. Screenshots added: 01-dashboard-empty, 02-lightbox-open, 03-arrow-nav, 04-annotate-pin, 05-esc-close, E1-server-down (all via `brain shots add --feature shot-review`). Standard review server left running on 4517, confirmed serving current code.
```

## Step 5 — 05-annotate-cta-toast

```
User feedback: no CTA after committing an annotation. Added post-commit toast to lightbox (opts.annotatedCta {text, actionLabel, action}); watch: 'Saved to the brain — brain shots notes shot-review' + Done annotating (closes); chrome: 'Queued in the composer — press Send' + Go to Send (closes, focuses sendBtn). Playwright smoke: TOAST text correct, BUTTON 'Done annotating', lightbox closed on click, JS-ERRORS none. Smoke-test annotation removed from annotations.json after.
```

## Step 6 — 06-annotation-delete

```
Added removeAnnotation(brain, slug, id) to brain-data.js (filters by id, throws clean Error on no-match or corrupt file, writes pretty JSON); executionContext now includes id in each annotation. Added POST /watch/<feature>/annotate/delete in server.js (handleWatchAnnotateDelete mirrors handleWatchAnnotate: same-origin guard, resolveWatch, JSON {id} body, 200 {ok:true}/400 {error}), registered above the /annotate route (both $-anchored, non-overlapping — order not load-bearing, confirmed). lightbox.js: opts.onDeleteAnnotation(annotation) -> Promise; .bl-pin-note switched pointer-events none->auto so hovering onto the note (a descendant of the hovered pin) keeps .bl-pin:hover satisfied; added Delete button + inline .bl-pin-note-err (reuses .bl-note-err styling pattern) inside the note; button stopPropagation prevents show-note toggle / lightbox close; on resolve splices the annotation out of the working array and calls renderPins(). dashboard.js: added deleteAnnotation() (same fetch/error shape as postAnnotation) and wired opts.onDeleteAnnotation into the lightbox open() call. chrome.js untouched by design (no onDeleteAnnotation passed; persisted pins stay read-only there) and confirmed it still works with the new id field present (ignored).

Verification:
- node --check passed on all 4 files.
- Temp-brain test (scratchpad): addAnnotation -> listAnnotations shows record -> removeAnnotation(id) -> listAnnotations == [] ; removeAnnotation(bad-id) threw exactly: no annotation "nonexistent-id" for feature testfeat ; corrupt annotations.json -> removeAnnotation threw: corrupt annotations JSON at <path>: Expected property name or '}' in JSON at position 1 (line 1 column 2). Temp brain deleted after.
- Fresh review server started on 127.0.0.1:4599 (BRAIN_AXI_PORT=4599 node lib/review/server.js) against the real .brain, leaving the pre-existing 4517 server alone (confirmed still healthy after). curl: POST /watch/shot-review/annotate created id mrp6yau6y0d82n -> GET context listed it -> POST /watch/shot-review/annotate/delete {id} -> {"ok":true} HTTP 200 -> GET context no longer lists it (ids back to the original 2). Unknown id -> 400 {"error":"no annotation \"does-not-exist\" for feature shot-review"}. Cross-origin (Origin: http://evil.example.com) -> 403 {"error":"cross-origin request refused"}. Malformed JSON body -> 400 {"error":"invalid JSON body"}.
- Playwright smoke (scratchpad, playwright installed there) against the fresh port-4599 server: created a temp annotation via curl on shot 02-lightbox-open.png (note "playwright-temp-pin"), opened /watch/shot-review, opened that shot in the lightbox, located the pin carrying the temp note, hovered it (note became visible), asserted the Delete control was visible inside the hovered note, screenshotted that state, clicked Delete, asserted the pin's DOM node detached and GET /watch/shot-review/context no longer listed the annotation. All assertions passed (ALL PLAYWRIGHT ASSERTIONS PASSED). Screenshot: /private/tmp/claude-501/-Users-sean-Desktop-personal-projects-brain-axi/50230514-E2AA-43E4-BC5A-B826DB2534F0/scratchpad/pin-hover-with-delete.png. Test server on 4599 killed after; .brain/features/shot-review/screenshots/annotations.json diffed byte-identical to its pre-test snapshot (my temp pin left no residue; the pre-existing 2 records, one already-superseded-by-recapture "button misaligned" pin and one stray "s" note, were untouched).
- git status --short: only lib/review/{brain-data,server,lightbox,dashboard}.js modified by me; .brain/features/feature_list.json, .brain/features/annotation-watch/, plans/2026-07-17-annotation-watch-runner.html remained in their pre-existing state, untouched.

Known limitation (not fixed, out of scope): a pin committed via Annotate in the SAME lightbox session (before a page reload) has no `id` yet — lightbox.js's commitPending() pushes the locally-built {shot,x,y,note} object on success, not the server's returned record with id. Deleting such a pin before reload would send id:undefined and fail with the "no annotation" error. Only pins loaded from a fresh context fetch (post-reload) carry id and are deletable. Did not touch dashboard.js's postAnnotation return contract to fix this since it wasn't in the requested scope.
```

## Step 7 — 07-send-to-claude

```
Added composer send step to the annotation pin flow.
Data model (brain-data.js): annotations gain sentAt (ISO|null); addAnnotation writes sentAt:null; new markAnnotationsSent(brain,slug) stamps sentAt on all unsent records regardless of superseded, returns {sent:n}, skips the write when n=0; listAnnotations normalizes missing sentAt to null; executionContext annotation mapping passes sentAt through.
Server (server.js): POST /watch/<feature>/annotate/send mirrors the delete handler (same-origin + resolveWatch + tolerant empty body) -> markAnnotationsSent -> 200 {ok,sent} / 400 on throw.
Fixed lightbox id-gap: commitPending now pushes the server's resolved annotation record (merged over local fields) when it carries an id, so fresh pins are immediately deletable; dashboard.js's postAnnotation now parses and returns body.annotation.
Lightbox send affordance (lightbox.js): opts.unsentCount + opts.onSendFeedback(); persistent "Send N pin(s) to Claude" topbar button (+1 per commit, -1 clamped on deleting an unsent pin); click disables button, calls onSendFeedback(), toasts "Sent — Claude will pick this up." or the error; toast generalized to showToastWith() shared by post-pin CTA and send flow, action ctx now {close, send}.
Dashboard wiring (dashboard.js): computes initial unsentCount from context annotations (sentAt==null && !superseded), passes unsentCount + onSendFeedback (sendAnnotations POST /annotate/send) to the lightbox; annotatedCta text -> "Pin saved — send your feedback to Claude when you're done." / actionLabel "Send to Claude" / action calls ctx.send().
CLI (bin/brain.js): cmdShotsNotes table gains a sent column (no / YYYY-MM-DD); summary line -> "notes: N annotations for <slug> (X open, Y superseded, Z unsent)"; openNotesByRel unchanged (still counts all open regardless of sent). skillContent() shots-notes bullet updated to describe the Send step and unsent-as-draft semantics; skill --write regenerated, skill --check green.

Verification (verbatim):
- node --check on brain-data.js, server.js, lightbox.js, dashboard.js, bin/brain.js: all OK.
- Temp-brain node test: addAnnotation x2 -> sentAt [null,null]; markAnnotationsSent -> {"sent":2}; listAnnotations after -> both sentAt stamped (ISO strings); addAnnotation x1 more -> sent=2 unsent=1; markAnnotationsSent again -> {"sent":1}; legacy record with no sentAt key -> reads sentAt:null. Temp brain deleted after.
- curl on fresh port 4901 with a temp brain: POST /annotate -> {"ok":true,"annotation":{...,"sentAt":null}} (id present); GET /context -> annotations[0].sentAt:null; POST /annotate/send -> {"ok":true,"sent":1}; GET /context after -> sentAt is an ISO string; POST /annotate/send again -> {"ok":true,"sent":0}; POST /annotate/send with a fully empty body (no -d) -> {"ok":true,"sent":0}; POST /annotate/send with Origin: http://evil.example -> {"error":"cross-origin request refused"}. Server + temp brain torn down after.
- Playwright against a fresh server on port 4902 + the REAL brain, feature shot-review (pre-existing legacy pins marked sent first so the baseline read 0 unsent, restored byte-for-byte afterward): pin a note -> toast {"text":"Pin saved — send your feedback to Claude when you're done.","action":"Send to Claude"}; topbar -> {"text":"Send 1 pin to Claude","display":""}; delete that pin -> button style.display becomes "none"; pin again -> topbar again reads "Send 1 pin to Claude"; click the topbar Send button -> toast "Sent — Claude will pick this up." and button hides; GET /context confirms the surviving test annotation's sentAt is a non-null ISO string. Screenshot saved (scratchpad) showing "Annotating…" + "Send 1 pin to Claude" side-by-side in the topbar with the numbered pin visible. Test annotation deleted via curl afterward and annotations.json restored from a pre-test backup; diff against the backup was clean (0 differences) and `git status --short` shows no diff under .brain/features/shot-review/.
- node bin/brain.js shots notes shot-review --brain .brain -> "notes: 2 annotations for shot-review (1 open, 1 superseded, 2 unsent)" with a sent column reading "no" for both pre-existing legacy records (as expected — they were restored to their pre-test, never-sent state).
- node bin/brain.js skill --check -> "skill: skills/brain/SKILL.md is up to date", exit 0.
- node bin/brain.js check --brain .brain -> 8/8 checks pass, exit 0.
- git status --short: only .brain/features/feature_list.json (pre-existing annotation-watch diff, untouched by this task), bin/brain.js, lib/review/brain-data.js, lib/review/dashboard.js, lib/review/lightbox.js, lib/review/server.js, skills/brain/SKILL.md modified; annotation-watch/ dir and the plans/*.html file remain untouched (??, from the other in-flight feature).

Deviation: lightbox's .bl-send-btn CSS class initially baked in "display:none" as part of the rule, which broke JS's style.display="" toggle (empty string resolves to the stylesheet's none, not visible) — caught by the Playwright run, fixed by removing display:none from the class and relying on JS-only visibility (matching .bl-annotate-btn's existing pattern).
```

## Step 8 — 08-annotations-section

```
node --check: dashboard.js OK, brain-data.js OK. Added Annotations section (dashboard.html #annotations + count-badge CSS) directly after Screenshots; dashboard.js renderAnnotations() sorts newest-first by 'at' when present (fallback: reversed file order), truncates note to 100 chars (title=full), shows shot basename+x/y muted, and a verify-chip-based badge (resolved/sent/draft). Refactored renderShots' inline lightbox-open into shared openLightboxAt(idx) using module-level shotsPayload; annotation rows call it via shotIndexForRel(rel). Section re-renders every render(ctx) call so SSE keeps it live. Added friendlyAnnotationError() in dashboard.js: HTTP 404 + {error:"not found"} (stale pre-route server) -> 'server is running old code — re-run brain watch <feature> and reload'; wired into postAnnotation/deleteAnnotation/sendAnnotations. brain-data.js executionContext now passes a.at through (at: a.at || null) for future sort-by-at once the server process restarts (module cache meant the live 4517 server still returned no 'at' during this session's testing -- verified via direct fresh 'node -e' import of executionContext(brain,'shot-review') showing at:'2026-07-17T05:28:36.708Z'). Playwright against live 127.0.0.1:4517 + real .brain feature shot-review: 1 pre-existing real annotation (button misaligned - test pin, 02-lightbox-open.png) rendered with 'resolved' badge + '1 resolved' heading badge + open badge hidden; row click opened lightbox on 02-lightbox-open.png (caption asserted); curl POST temp annotation on 01-dashboard-empty.png appeared as new row WITHOUT reload via SSE (2 rows), badge 'draft', '1 open' badge shown; row click opened lightbox on 01-dashboard-empty.png; UI Delete (hover pin -> .bl-pin-delete click) removed pin and row disappeared without reload (1 row); server context confirmed via fetch no longer lists temp id; zero JS console/page errors across the whole flow. Fixed one bug found during testing: .count-badge{display:inline-block} in dashboard.html was overriding the [hidden] attribute (author CSS beats UA default regardless of specificity) -- added .count-badge[hidden]{display:none} so annoOpenBadge/annoResolvedBadge actually hide. git status --short: only lib/review/brain-data.js, lib/review/dashboard.html, lib/review/dashboard.js modified; real annotations.json and forbidden annotation-watch files untouched.
```
