# harness-mode run — 2026-07-23

## Step 1 — verify --stage verify

```
results[2]{check,status,exit,seconds}:
  skill-sync,pass,0,0
  harness,pass,0,0
```

## Step 2 — phase-1-committed

```
results[2]{check,status,exit,seconds}:
  skill-sync,pass,0,0
  harness,pass,0,0
summary: "2/2 pass"
help[2]:
f19854a feat: brain verify — declared project-check runner (harness-mode phase 1)
```

## Step 3 — phase-2-committed

```
playbooks[5]{id,use_when}:
  start,"starting any non-trivial task — frame it, read the brain, baseline, open state"
  plan,writing any plan/proposal/design artifact for human review
  verify,verifying a user-visible feature works — browser walk with screenshot evidence
  execute,implementing an approved plan / working a feature to shipped
  done,"before declaring any task complete — full verify, harness invariants, coherence"
help[1]: Run `brain playbook <id>` to see the full playbook
87d1a7d feat: start/done bookend playbooks (harness-mode phase 2)
```

## Step 4 — phase-3-committed

```
init --dir <scratch> --agents-md: created[13] root-relative incl. AGENTS.md + CLAUDE.md -> AGENTS.md symlink; scratch check all-pass, verify 1/1 pass; re-init opError exit 1; repo regressions: skill --check 0, verify 2/2, check all-pass. f86c271 feat: brain init — minimal .brain skeleton scaffolder (harness-mode phase 3)
```

## Step 5 — verify --stage verify

```
results[2]{check,status,exit,seconds}:
  skill-sync,pass,0,0
  harness,pass,0,0
```
