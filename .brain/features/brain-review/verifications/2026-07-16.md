# Verification: Execution dashboard (brain-review /watch surface)

- **Slug**: `brain-review` (feature memo: `.brain/features/brain-review/brain-review.md`)
- **Date**: 2026-07-16
- **Verified by**: Claude Opus 4.8 (Playwright — headless chromium script, fresh profile)
- **Base URL**: http://127.0.0.1:4517 _(review server: started by agent via `brain watch brain-review --no-open`)_
- **Role**: n/a — loopback-only local tool, no auth surface
- **Verdict**: ✅ PASS — golden path (dashboard render + live SSE update) and error path (unknown feature 404) both worked; no app jsErrors.

## Golden path

_Human opens the execution dashboard after plan approval and watches the agent work._

| # | Step | Expected | Observed | Screenshot | Result |
|---|------|----------|----------|------------|--------|
| 01 | `brain watch brain-review` → open `/watch/brain-review?brain=…` | Full dashboard: pipeline at current stage, health strip, run-step accordion, verification/checkpoint/screenshot sections, live dot | Pipeline showed "run steps" current (in-progress + steps, no verifications/pr yet); "✓ harness ok (8 checks)"; 5-step accordion with verbatim observed output, latest open; checkpoint feed incl. plan-approval entry; empty states for verifications/screenshots; footer dot "live" | [`01-dashboard-live.png`](../screenshots/01-dashboard-live.png) | ✅ |
| 02 | `brain runs append` while page open | New step appears via SSE without reload | `summary` count 5 → 6 within 5s of the append; marker step text visible; no navigation event | [`02-live-sse-update.png`](../screenshots/02-live-sse-update.png) | ✅ |

## Error path

_Unknown feature slug in the /watch URL._

| # | Step | Expected failure surface | Observed | Screenshot | Result |
|---|------|--------------------------|----------|------------|--------|
| E1 | GET `/watch/nope?brain=…` | 404 JSON with known slugs, no stack trace | HTTP 404, body `{"error":"unknown feature: nope","help":"Known features: core-cli, brain-review"}` | [`E1-unknown-feature.png`](../screenshots/E1-unknown-feature.png) | ✅ |

## Console

- **jsErrors** — one entry: `Failed to load resource: the server responded with a status of 404 (Not Found)` — fired on the E1 navigation itself (the browser logging the expected 404 response); not app code. No jsErrors on golden-path steps.
- **networkErrors** — `404@E1-unknown-feature` (expected: that step asserts the 404). None on golden-path steps.

## Findings for main thread

- `networkidle` never fires on the dashboard (SSE holds the connection open) — drivers must use `waitUntil: "load"` + settle timeout. Noted here so future verifier scripts don't trip on it.
- Otherwise none.

## Verdict rationale

Both paths were driven in a live headless browser against the real server spawned by the real CLI. The golden path proved the full render (all sections, correct stage derivation, health strip green) and the core promise — live updates: a `runs append` issued while the page was open appeared in the DOM via SSE within 5 seconds with no reload. The error path returned a friendly 404 JSON listing known slugs. The single console 404 entry is the expected error-path response being logged by the browser, not an application defect.
